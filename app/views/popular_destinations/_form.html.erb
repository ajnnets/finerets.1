<%= simple_form_for(@popular_destination) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :area %>
    <%= f.association :region %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
